# .github/workflows/publish.yml
name: Publish to PyPI

on:
  push:
    tags:
      - "v*"  # Triggers on tags like v0.1.0, v1.2.3, etc.
  workflow_dispatch:
    inputs:
      testpypi_only:
        description: 'Publish to TestPyPI only (for testing)'
        required: false
        default: false
        type: boolean

jobs:
  publish:
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/apathetic-logging
    permissions:
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"

      - name: Install Poetry
        uses: abatilo/actions-poetry@v4
        with:
          poetry-version: "latest"

      - name: Install dependencies
        run: poetry install --no-interaction --no-root

      - name: Build package
        run: poetry build

      - name: List built packages
        if: github.event.inputs.testpypi_only == 'true'
        run: ls -lh dist/

      - name: Publish to TestPyPI
        if: github.event.inputs.testpypi_only == 'true'
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          verbose: true  # Enable verbose output to see detailed error messages
        continue-on-error: true  # Continue if version already exists or other errors occur

      - name: Publish to PyPI
        if: github.event.inputs.testpypi_only != 'true'
        uses: pypa/gh-action-pypi-publish@release/v1
